<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.TVPlayer" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>

    <style name="Theme.TVPlayer.FullScreen" parent="Theme.TVPlayer">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.TVPlayer.Settings" parent="Theme.TVPlayer" />

    <style name="Theme.TVPlayer.Dialog" parent="Theme.MaterialComponents.Dialog.Alert" />

    <style name="Theme.Leanback.Search" parent="Theme.Leanback">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>